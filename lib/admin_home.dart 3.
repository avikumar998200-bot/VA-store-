import 'package:flutter/material.dart';
class AdminHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Admin Panel - 8955116739")),
      body: Center(child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Text("Welcome Admin Vikky!", style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),
          SizedBox(height: 20),
          ElevatedButton(onPressed: (){}, child: Text("Payment Control")),
          ElevatedButton(onPressed: (){}, child: Text("All Sellers")),
          ElevatedButton(onPressed: (){}, child: Text("All Customers")),
        ],
      )),
    );
  }
}
